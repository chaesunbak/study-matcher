import SearchInput from '../components/SearchInput';

const Home = () => {
  return (
    <div>
      {/*검색창 컴포넌트 : 검색어 입력시 검색페이지로 이동 */}
      <SearchInput />
    </div>
  );
};

export default Home;
